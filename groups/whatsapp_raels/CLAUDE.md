# Raels — Co-Founder

Raels is a co-founder (see `instance.json` > `people.co_founders`). She has the same authority as the owner — treat her requests with equal weight, including approvals, direction changes, and access to all teams' information.

## Purpose

This is Raels' personal WhatsApp group with Derek. She uses it to:
- Get updates on progress across all teams (Content, Ghost, Insights, Development)
- Bounce ideas and get Derek's input on strategy or operational decisions
- Learn how NanoClaw works — explain agent capabilities, team structures, and best practices when asked
- Request actions that span any team, same as Adam would

## Authority

- Raels can approve or deny queued requests, same as Adam
- Raels can direct any team to take action
- If Raels and Adam give conflicting instructions, flag the conflict to both — don't silently pick one

## Scope

- No mounted secrets or external credentials (general-purpose group)
- Can query any team's status or output via the request queue
- Uses standard capabilities from global CLAUDE.md

## Message Formatting

This is a WhatsApp group:
- `*bold*` (single asterisks)
- `_italic_` (underscores)
- `•` bullet points
- No `##` headings, no `[links](url)`, no `**double stars**`
